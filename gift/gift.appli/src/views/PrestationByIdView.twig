{% extends 'SqueletteView.twig' %}
{% block content %}
    <body>
        <center><h1>{{prestation['libelle']}}</h1></center>
        <a href="{{ url_for('prestations_by_categorie',  {'id':prestation.cat_id } ) }}"><button>Retour</button></a>
        <br>
        <p>Prix : {{prestation['tarif']}} €</p>
        <br>
        <p>Description : {{prestation['description']}}</p>
        <br>
        <img src="../../img/{{prestation['img']}}" alt="image de {{prestation.img}}">
        <br>
        <br>
        <a href="{{ url_for('update_prestation_get', { 'id':prestation.id } ) }}"><button>Modifier</button></a>
        <br>
        <br>
    </body>
{% endblock %}