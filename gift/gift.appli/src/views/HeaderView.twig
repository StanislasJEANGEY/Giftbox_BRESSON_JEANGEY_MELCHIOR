<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mon site</title>
    </head>
<body>
<header>
    <h1><a class="title-button" href="{{ url_for('accueil') }}">GiftBox</a></h1>
    <!-- Si l'utilisateur n'est pas connecté, afficher le bouton de connexion -->
    {% if not session['logged_in'] %}
    <a class="title-button" href="{{ url_for('connexion_get') }}">Connexion</a>
    {% endif %}
</header>
</body>
</html>
